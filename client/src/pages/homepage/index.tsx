import Events from "./Events";

export const HomePage = () => {
  return (
    <div>
      home page placeholder
      <section id="#about">
        <Events />
      </section>
    </div>
  );
};

export default HomePage;
